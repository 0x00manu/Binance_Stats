#!/bin/bash
echo "adding  prefix to all file(s) in current folder"
#for i in *; do mv ${i} IBinance${i}; done

echo "replacing all the instance word in all the files in current folder"
#sed -i 's/class/interface/g' *
#stream editor||search class|replace interface| globally (not just first instance)|* all files|